# Codex 门票钉住原理

> 本文对应 [mracry/sub2api](https://github.com/mracry/sub2api) **魔改版**，不是 tosky / ranxi2001 原版。本仓库不向原仓库提 PR。
> 本魔改无偿分享。用得上的话，请给仓库点个 Star：https://github.com/mracry/sub2api

292/312 只是 `x-codex-turn-state` 的观测形态，不是模型质量。打票不承诺更容易拿到票。

## 一句话

官方把门票绑在「出口 IP + 会话身份」上。  
网关先用 ping 打到 292，再让后续对话原样走同一出口、同一身份；对话和打票不能同时跑。

## 官方看什么

| 东西 | 含义 |
|---|---|
| 292 | 个人号合格票（约 10 块） |
| 312 | 被踢后的形态（约 11 块），`response_mismatch` |
| HTTP 200 | 仍可能在流里把票踢成 312，不能只看状态码 |

票本身存在 `x-codex-turn-state`。官方每一轮会换一个新 state，下一轮必须带上返回值，不能一直重放打票那一包。

## 为什么会被踢

打到 292 之后，如果对话和 ping 不像同一次会话，官方就作废：

1. **换了出口**：账号代理、别的节点、负载均衡轮换。
2. **换了身份**：对话改写 `session_id`、指纹、`client_metadata`、`prompt_cache_key`、UA、beta、routing、OpenCode session。
3. **头和体不一致**：头钉了打票 session，体还是隔离/指纹身份。
4. **边聊边打**：hunt ping 会转 `x-codex-turn-state`，把正在用的 292 烧掉。
5. **旧窗口接着用新票**：旧对话里的加密项绑的是旧身份，对不上。

常见假象：第一轮能聊，后面长流 EOF，再下一轮立刻 312。

## 网关钉什么

打票阶段：多节点探测，直到收下 292。记下出口节点、代理、session。有活票就停探，不再继续烧。

对话阶段，整条 HTTP/WS 都按这张票走：

- 出口钉在打票节点（Selector，禁止账号代理兜底）
- session / 身份跟 ping 一致，剥掉对话额外头
- 不写 `client_metadata` / `prompt_cache_key`
- 跟随官方返回的 `x-codex-turn-state`
- 对话期间对该账号暂停打票

## 怎么用

| 情况 | 做法 |
|---|---|
| 刚打出新 292 | 新开一个对话窗口 |
| 同一张票还活着，接着聊 | 继续当前窗口，不要再开第二个 |
| 票过期，重打了一张 | 换新窗口，从空白聊 |
| 被踢成 312，再打一张 | 换新窗口 |
| 正在聊天 | 不要点「恢复打票」，不要手动打票 |

不是每轮都要新开窗口。只有身份断过（新票、过期重打、被踢、旧窗脏了）才开。

## 过期重打要不要换窗口

要。过期重打出来的是一张新票，旧窗口不要接着用。

两件事别混：

- **同一张票还没过期**：官方每轮会换 `x-codex-turn-state`，网关跟着转。这还是同一条会话链。继续当前窗口。
- **票过期后重新打到一张 292**：这是一次新的 ping，拿到的是新 turn-state。旧窗口里还堆着上一张票聊出来的加密项、`previous_response_id`。把新票塞进旧窗，官方对不上，轻则 `invalid_encrypted_content`，重则 312。

session 就算碰巧钉在同一节点上，turn-state 和加密 lineage 也已经是新链了。过期重打 = 新开一窗。

## 实测

20x、gpt-6-astra，钉住打票出口和身份后，同一窗口连续对话约 30 分钟，票仍是 292，没有被踢。